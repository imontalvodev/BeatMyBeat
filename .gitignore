# Monorepo: solo versionar la app Android (carpeta app/).
# backend/ y middleware/ son legado y no deben volver al repo.
*
!.gitignore
!README.md
!app/
!app/**

# Legado explícito (por si se recrean carpetas locales)
/backend/
/middleware/

# Artefactos y secretos (en cualquier ruta)
*.apk
*.aab
*.jks
*.keystore
**/release/
local.properties
.env
!.env.example
